# 第二階段完整交接與組件修正

使用者已授權進入第二階段，並要求確保開發接收完整資訊、持續執行。第一階段已驗收及使用者確認，第三階段未授權。

## 必讀與優先順序

1. 本文件：最新使用者反饋、工作銜接與完成標準。
2. 07_第二階段代理後台_執行與驗收基準.md：九模組功能與 AG2-01～10 全部必做。
3. 04_三後台整合_第一階段執行基準.md、06_第一階段獨立驗收報告.md：已通過架構及角色隔離，不可回退。
4. 03_代理與商戶後台_原型規格基線_V0.2.md 第5節：代理細節參考；獨立網站要求已被統一入口取代，不能再採用。

## 使用者最新反饋

「但我看實作的地方只有匯率報表，並且好像沒有用組件做，是用刻的」
「他好像沒有在執行，請確保他都有接收到完整資訊」

## 已核實現況

- game-admin/src/router/modules/partnerPortals.ts 仍連 stage-page 與第一階段 dashboard/exchange-report，尚未連到新增代理頁。
- src/views/partner-portals/agent 已有 dashboard、relationships、merchants/index+detail、business-terms、reports、exchange-rates 檔案；不等於頁面已可用。
- 對帳/結算、公告通知、帳號權限尚需補齊。
- shared.scss 有自製 agent-kpi 與 agent-descriptions，詳細使用 dl/dt/dd，與沿用既有組件的要求不足。
- 資料 store 已有 agentPortal.ts，需驗證其資料來源與權限，而非只按檔案存在判定完成。

## 優先修正

1. 先讀總後台既有同類頁與元件 API：ArtSearchBar、ArtTable、ArtTableHeader、ArtStatsCard、AppPageHeader、ElDescriptions，選擇適用的既有組件。
2. 既有組件能處理的搜尋、表格、分頁、統計卡、詳細資料，不另刻平行外觀；必要業務容器可補少量樣式，不能重造一套後台。
3. 將可用頁逐一接上同站點 /agent/* 路由並實際點擊確認；不要所有檔案寫完才接線。
4. 九模組完整範圍仍依 07，不因優先修路由而刪減後續功能。
5. 每個頁面完成需具備：路由可進入、元件與總後台一致、主要互動可操作、權限及資料口徑符合規格。
6. 僅維持 3006 主要本地服務，若停掉需恢復並查HTTP/實際頁；3011/3012 只是舊原型。

## 開發工作規則

先回傳接手確認（已讀文件、理解的9模組、組件要求、目前優先工作、有無阻礙），隨後繼續實作，不以回覆接手作為工作終點。將即時進度寫在 09_第二階段開發進度.md，按『頁面/路由/使用組件/互動完成/測試/待辦』記錄，可查檔案及證據。

沿用現有未提交工作樹、先保留回復點，不 reset/stash 或整包覆蓋。不可進第三階段、不 push/部署、不自行改算法或擴權。未知角色拒絕與登入清理不可回退。

完成後將09進度、驗證結果、真實本地連結、帳號、組件對照、截圖及已知限制回傳溝通任務 01a06cda-773a-7961-adfc-dc8b41f1b4bd。規格核對後交 asg驗收選手獨立驗收，問題修復複驗，最後由使用者確認才進第三階段。
