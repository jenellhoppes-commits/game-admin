# 第二階段實際進度（2026-09-07）

## 儀錶板與匯率 UI 試版

- 使用者同意試做：儀錶板改全寬分幣表格、待辦橫排（窄螢幕換行），全部幣別時隱藏金額空卡，單一幣別才顯示數字摘要。
- 匯率列表改純數字且不換行，保留基準／目標幣獨立欄；日期整併，版本可懸停查看完整文字。鎖定時間與完整算式保留於詳細頁。匯率維持唯讀。
- 修改前副本：audit/dashboard-before-compact.vue、audit/rates-before-compact.vue。待使用者確認試版；原業務邏輯待修項不因此判定完成。

## 使用者核准的代理報表 UI 調整

- 僅修改代理報表頁：分析维度改頁籤、ArtSearchBar 整理查詢、ArtTable 呈現列表及下方分幣總計，移除大型幣別卡片與重複說明。
- 桌機首屏可見列表；原幣獨立一欄、金額靠右與千分位，顯示精度取自既有幣別設定。窄畫面表格可橫向捲動。
- 已實測商戶頁籤切換；TWD 查詢同步將列表及總計改為一筆，重置恢復15筆；390px手機查詢單欄，1280px明暗模式已視覺檢查。
- 原有日期/時區及遊戲維度資料缺陷仍屬待修，這次 UI 交付不代表其業務邏輯通過。等待使用者確認本頁版型後才套用其他頁面。

目前狀態：開發中，未通過階段驗收。不得以網站已開啟或檔案已存在作為完成依據。

## 本次直接修正

- 接上六模組：代理儀錶板、代理關係、商戶管理、商務條件、代理報表、唯讀匯率報表。
- 補商戶詳細、匯率快照詳細路由，以及 /agent/exchange-rates 別名。
- 代理頁表格統一使用 ArtTable，主列表使用組件內分頁（20/50/100）。
- 儀錶板統計卡使用 ArtStatsCard，三處詳細資料使用 ElDescriptions；匯率查詢使用 ArtSearchBar。
- 修正篩選下拉寬度、代理樹節點重疊。
- 保留修改前副本於 audit/agent-stage2-before-route-fix。

## 已核實

- TypeScript 型別檢查通過。
- Vite 完整建置通過。
- 第一階段導覽與身分隔離測試通過；選單測試依實際選單排除隱藏詳細路由。
- 使用正常登入流程，以 Agent 登入 3006，看到代理儀錶板、代理關係樹與節點摘要、商戶列表與分頁。
- 這些只代表本次接線與組件修正，尚非九模組業務驗收通過。

## 必須繼續完成

1. 對帳／結算、公告通知、帳號與權限三個頁面仍未實作，仍是階段占位。
2. 儀錶板與代理報表的日期／時區目前未影響來源資料，必須修正後才可驗收。
3. 遊戲維度目前是依列索引分配類別名稱，不能作為有效遊戲分析，需改為真實同源彙總。
4. 全部搜尋區逐一核對並沿用既有搜尋組件；詳細返回需保留查詢與頁碼。
5. 共用頁首仍顯示總後台待審核數量，需隔離代理可見內容。
6. 權限撤銷、申請不提前生效、快照範圍及結算確認門檻仍需完整測試。
7. 桌機／手機、明暗模式與獨立驗收尚未完成。

## 本地入口

http://127.0.0.1:3006/#/agent/dashboard

同一登入頁選代理後台，示範帳號 Agent，密碼 123456。舊 3011／3012 不作本次交付。

不进入第三階段，不 push 或部署，不恢復安排選手全面追蹤。全階段完成及驗收通過後仍須使用者確認。

## 增量驗收回報後修正

- 修正 `/agent/exchange-rates` 被空選單過濾導致 404：保留同代理角色樹下的隱藏相容路由並指定既有組件；重新載入後實際重導至 `/agent/reports/exchange-rates`。
- 代理儀錶板及報表的日期區間增加開始／結束日期標籤；共用 ArtTable 為內建筆數選擇器补可讀名稱。
- 報表時區欄加寬，桌機可完整看到 Asia/Taipei (UTC+8)，手機仍沿用全寬規則。
- 型別及第一階段回歸測試通過；已於瀏覽器看到相容網址重導、結束日期與每頁筆數的可讀名稱，以及完整時區文字。
- 以上三項已回交獨立複驗；第二階段整體仍未通過。

## 2026-09-07 參考換算（使用者已確認）
- 代理報表新增共用 ReferenceCurrencyPreview 組件，選擇參考幣別後顯示全部已套用查詢結果的投注、派彩、遊戲輸贏估算。
- 固定標示【該金額只可參考】，保留原幣列表、分幣總計及原幣 CSV；不影響對帳／結算。
- 採截至當日最新 Locked 正式匯率，以 USDT 交叉換算，顯示各採用匯率日期。缺任一必要匯率不顯示部分合計；同幣不換算。
- 型別檢查通過；換算案例已核對交叉幣、同幣、缺匯率、未發布及未生效匯率。瀏覽器已選 USD 並看到三項預覽金額及日期。
- 主題切換移除強制重建頁面，避免清空查詢狀態；待獨立複驗。
- 本次是代理報表增量 UI；既有日期篩選與遊戲維度等問題仍未完成，第二階段尚未整體驗收。

## 2026-09-07 第二階段持續實作（取代前述未實作清單的現況）
- 已新增並接通對帳／結算、公告通知、帳號與權限三頁，沿用 ArtSearchBar、ArtTable、ElDescriptions、Dialog/Drawer。
- 代理側欄九項提升一層，路由與權限樹保留；代理及商戶頁首不再顯示總後台審核入口。
- 對帳僅自身單據，差異須同幣及可回報狀態；確認檢查快照、差異及旗下商戶狀態。確認不表示付款或鎖定。
- 公告提供閱讀篩選、詳細及已讀；既有摘要 seed 補標示清楚的示範正文，未編造正式維護日期。
- 帳號提供邀請、角色設定及啟停；同帳號禁止重複邀請，自身禁止角色/啟停操作，Pending 不可直接啟用。邀請僅原型紀錄，不寄信、不建立正式登入資格。
- 開發已修正報表來源為 transactionCenter.bets：84 筆授權來源交易，日期 2026-08-25～31，真實遊戲與原始金額，去重線路、局數、线路會員。日期篩選已影響儀錶板、報表、總計、CSV及參考換算；原預設九月因此正確空資料。
- 商戶、關係、條件搜尋區已改用 ArtSearchBar；商戶詳細返回帶回查詢與頁碼。
- 參考換算已依驗收回報修正 ElStatistic 截斷問題，使用與列表相同 Intl 格式化。
- 型別檢查、第一階段導覽/角色隔離測試已通過。開發另完成聚合一致性測試；新頁獨立驗收進行中。
- 未宣告第二階段通過：新增頁操作、手機/明暗、撤權範圍及申請完整流程仍待集中複驗。跨後台審核聯動屬第四階段；未進第三階段、未部署。
- 集中驗收中發現格式化移除了模板內多語句分號，已將分頁及搜尋更新移至具名函式；六個頁面的 Vue template 重新編譯通過。已通知验收續驗，目前固定版本。

## 2026-09-07 搜尋排版與日期快捷修正
- 根因：ArtSearchBar 預設 70px 右側欄名導致五字名稱換行。代理搜尋區統一上方欄名、輸入寬度與操作按鈕底部對齊；移除商戶搜尋雙層卡片，補相鄰卡片間距，移除對帳頁對所有按鈕的廣泛 margin。
- 共用搜尋改為 emit 新 model，避免 literal/computed model 無更新；空字串保留於編輯狀態，僅查詢 payload 清洗，避免 .trim() 對 undefined 出錯。
- 已於瀏覽器驗證帳號搜尋、商務條件無匹配再清空恢復；桌機及390px手機搜尋標籤完整。
- 依新授權在代理報表、代理儀錶板及總後台報表日期面板新增：本日、昨日、本周、上週、本月、上月。台北時區、週一起算，本期至今天，上一期完整；手機快捷項三欄與月曆垂直排列可捲動。
- 日期邊界自測包含跨年、閏月、台北午夜與上週，均通過；八頁模板編譯、型別與第一階段測試通過。
- 既有 confirmed-defects.test.ts 仍引用已移除的 supplierReconciliations 而失敗，屬過時測試，未宣告該回歸套件通過。
