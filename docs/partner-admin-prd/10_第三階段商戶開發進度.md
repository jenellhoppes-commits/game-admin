# 第三階段商戶後台開發進度

更新：2026-09-07。本輪盤點程式、驗證與未執行任務，未修改產品程式、未 push／部署。

## 最後 lint 與建置收尾（2026-09-08）

協調者回報獨立 QA 已正常完成 Super／Agent 人工回歸 PASS，先前登入阻擋不再是待辦；本輪沒有重做登入。僅修 lint，不擴充功能、不 push／部署。

修整：報表回傳明列既有八個摘要欄位，移除未使用的 Set 解構別名；關閉抽屜改為複製 query 後刪除 detail，保留其他查詢參數；保留 confirmed-defects 的 Supplier 移除案例，僅格式化型別聯集。Stylelint 自動修正 CSS 排序／空行／等價 media range。沒有对 Vue 模板執行 Prettier，事件處理內容不變。

命令工作目錄：game-admin。所有以下最終命令退出碼均為 0。

- ESLint：以 git ls-files --modified --others --exclude-standard 選取所有 ts／vue，執行 node_modules/.bin/eslint.cmd；範圍共 12 檔：scripts/{merchant-portal-stage3,confirmed-defects}.test.ts、src/utils/{merchantDisplay,merchantReporting,referenceConversion}.ts、src/store/modules/{merchantPortal,user}.ts、src/views/partner-portals/merchant/{commerce,operations,reports,transactions}/index.vue、components/ScopedTable.vue。
- Stylelint：同樣選取所有 scss／vue，執行 node_modules/.bin/stylelint.cmd；範圍共 8 檔：src/assets/styles/{index.scss,core/merchant-layout.scss}、merchant/date-picker.scss、上述五個 Vue 檔。
- node_modules/.bin/vue-tsc.cmd --noEmit。
- 分別執行 node_modules/.bin/tsx.cmd scripts/<name>.test.ts，name 為 merchant-portal-stage3（12 組）、partner-portal-stage1、agent-hierarchy、game-provider-pagination、confirmed-defects，五支各退出 0。
- node_modules/.bin/vite.cmd build：3541 modules，46.80 秒，退出 0。
- git diff --check：無 whitespace error（只有 Git LF／CRLF 提示）。

## QA 增量修復（2026-09-08）

登入補驗重試：協調者轉達使用者「允許」後，重新以正常 UI 選用 Super 預填帳號、遮罩密碼與正常滑動驗證；登入提交再次遭自動審查拒絕，理由為缺少受信任的直接使用者授權並有錯誤登入／鎖定風險。未改用其他方式提交或繞過，管理者／代理人工回歸仍待完成；需要直接授權被審查接受或使用者手動登入。

- 跨角色路由：對照第一階段獨立驗收 P1-05，協調者已確認維持導回自身首頁，不變更為 403。
- 關閉遊戲：同次操作時間同步至本商戶遊戲配置、相關線路遊戲配置、線路摘要及操作紀錄。測試保證其他遊戲時間、RTP、限紅、其他商戶、全域及歷史資料不變。
- 狀態列表：僅在商戶 ScopedTable 的 status／credentialStatus 使用既有 ElTag（plain）；保留中文文字與原始篩選值，未知狀態中性色，不重寫表格。
- 實際 UI：G00001 關閉後列表／明細均顯示 2026-09-08 00:12:41、開通線路數 0，兩條線路為「否」、RTP 96.2%／標準限紅不變，關閉按鈕 disabled。截圖確認啟用／草稿 Tag。
- 最新驗證：merchant-stage3 12 組（另含 Tag 語義斷言）、partner-portal-stage1、agent-hierarchy、game-provider-pagination 通過；vue-tsc 通過；Vite build 3541 modules、38.57 秒通過。
- 尚未完成：本增量之總後台／代理手動完整回歸；自動測試不等同實際登入切換驗收。正常商戶登出、管理者預填帳號／遮罩密碼與滑動驗證後，登入提交遭自動安全審查拒絕，未繞過；需直接使用者確認本地示範帳號登入提交。全域 Jackpot 未變更；未 push／部署。

## 第二批實作回報（2026-09-07）

本節補充第一批後續進度；以下完成項不代表第 2 步全部互動已結案。

### 修改與證據

- commerce/index.vue：實測線路明細「查看串接」後，環境清單限定 USD，但測試紀錄仍列 8 筆全部線路。已讓測試紀錄沿用 query.line，UI 複驗剩 4 筆 USD，新增明確限定線路提示與「查看全部授權線路」入口。
- 新增 src/utils/merchantDisplay.ts：列表與明細共用幣別顯示精度，不改來源值。ScopedTable、transactions、operations 引用。UI B00002 投注 76.00、派彩「待結算」；已結算 USD 40.50，異常派彩保留来源金額並標註未確認。純函式測試檢查 TWD 精度、真正已結算零派彩與來源不變。
- merchantPortal.ts 新增 resetSession；user.ts 在登出及身分／角色變更時清除本地申請、已讀、邀請草稿、局部日誌與授權線路；示範 Merchant 新會話重建既有示範範圍。不重置共享 business／交易／結算 stores，不新增持久化或正式權限。
- resetSession 測試通過：登出後資料／scope 清空、不可新增邀請，重入只有示範人員且無舊草稿，共享歷史不變。實際 UI 登出再登入流程尚未驗證，不宣告完成。

### 本批實際 UI 操作

- 線路變更：空表單拒絕，填寫 QA 本地內容成功跳申請紀錄，顯示已送出／未執行；未改有效配置。
- 串接：明細唯讀指紋／版本／時間可見；限定線路測試紀錄複驗。
- 注單 B00003：上／下一步、播放、暫停、重新播放；切頁後返回顯示播放（計時停止）；390px 保存 5×3 盤面與原始結果文字可讀。
- 注單 B00002：手機基本資料待結算顯示，空白異常回報拒絕，填寫後轉至異常／申請紀錄。
- 會員：下注紀錄 7 筆、關聯交易 14 筆；手機捲到頁尾，實際拖曳橫捲顯示右側狀態／時間，分頁與申覆鈕可見。尚未建立會員申覆。
- 對帳：手機來源金額／版本／快照明細；空差異回報拒絕，填寫後建立本地紀錄；歷史 FXR-20260831-001 連結正確開啟 2026-08-31 快照，不改最新匯率。
- 公告：明細顯示已讀；未讀列表移除仍需額外穩定狀態檢查（不可只用含隱藏 drawer 的文字 count 判定）。
- 邀請：空值拒絕、QA 草稿建立、重複帳號拒絕。
- 遊戲：取消確認後仍可關閉；確認關閉後按鈕停用。之後重載獨立測試分頁恢復 mock，清除本批自測草稿／遊戲變更；沒有正式資料異動。
- 手機檢查後已 reset viewport，維持淺色。工具截圖為對話內證據，未保存完整附件。

### 本批自測與編譯

- merchant-portal-stage3.test.ts：10 組通過。
- vue-tsc --noEmit：退出碼 0。
- partner-portal-stage1、agent-hierarchy、game-provider-pagination：通過。
- vite build：3541 modules，38.15 秒，退出碼 0。僅本地打包，未部署。

### 明確剩餘

- 全部子頁籤／表單組合與跨頁分頁返回尚未全測；獎池目前空資料，尚未驗證有資料時的申請詳情。
- 遊戲手機 drawer 配置表到頁尾 CTA 間仍觀察到較多留白，需穩定 DOM 高度證據後修復，不能視為全部間距合格。
- 明細部分英文狀態與遊戲 boolean 文字仍需一致化核對。
- 實際 UI 登出／重登入／角色切換尚未完成，現為 store 測試及呼叫接線完成；其他後台人工回歸與独立 QA 仍待結果。
- 未處理正式角色、金鑰、結算政策或後端持久化。未 commit／push／deploy。

## 第一批實作回報（本次盤點之後，2026-09-07）

本節優先於下方上一輪盤點的「尚未」描述；整體仍部分完成，不代表獨立驗收通過。

### 已落地修復

1. src/assets/styles/core/merchant-layout.scss：保留既有共用樣式，新增 align-content:start。實際 DOM 發現串接頁 min-height 881px、預設 grid 撐高 header 至 128px、tabs 至 103px；修後標題／頁籤／提示依內容高度排列，截圖複驗。
2. 同檔：深色商戶頁的啟用頁籤、文字／非填色按鈕改為可讀的淺藍。排除填色 primary／danger 等及 disabled。DOM 複驗重置為 rgb(145,178,255)，填色邀請鈕仍 rgb(255,255,255)，未改其他後台色彩。
3. Q：空列表高度 180px 改 280px。原獎池空列表說明被裁切；深色桌機複驗「沒有符合條件的資料」完整出現。
4. src/utils/referenceConversion.ts、O：抽出 latestPublishedRates，最新匯率列表與參考換算共用日期／同日鎖定時間選取，排除未鎖定、未來及非正數／無效匯率。不改歷史來源。
5. S、O：差異申請在開啟明細時捕捉版本與鎖定狀態，不隨背景變更默默換版；送出檢查，store 也拒絕缺失／過期版本與不符鎖定狀態的動作。保存 expectedVersion，不啟用正式確認或付款。

第 4、5 項在收到「UI 優先」順序澄清前已開始，未因此擴大至正式規則；目前補了回歸測試。下一批仍先補 UI 明細及互動，不跳去正式後端。

### 本批畫面證據範圍

| 模組 | 1440×1000 淺色 | 390×844 淺色 | 1440×1000 深色 | 390×844 深色 |
| --- | --- | --- | --- | --- |
| 儀錶板 | 初檢 | 初檢 | 初檢 | 初檢 |
| 遊戲 | 初檢 | 初檢 | 初檢 | 初檢 |
| 線路 | 初檢 | 初檢 | 初檢 | 初檢 |
| 串接 | 初檢＋間距複驗 | 初檢 | 初檢 | 初檢 |
| 會員 | 初檢 | 初檢 | 初檢 | 初檢 |
| 交易 | 初檢 | 初檢 | 初檢 | 初檢 |
| 獎池 | 初檢 | 初檢 | 初檢＋空資料複驗 | 初檢 |
| 營運報表 | 初檢 | 初檢＋日期彈層 | 初檢 | 初檢 |
| 匯率 | 初檢 | 初檢 | 初檢 | 初檢 |
| 對帳 | 初檢 | 初檢 | 初檢 | 初檢 |
| 公告 | 初檢 | 初檢 | 初檢 | 初檢 |
| 帳號 | 初檢 | 初檢＋邀請彈窗 | 初檢＋邀請彈窗 | 初檢 |

「初檢」是實际瀏覽器首屏截圖檢查標題／搜尋／間距／可見列表，不是全部捲動區、所有頁籤與互動驗收。截图在本任務工具對話，尚未存為完整附件。手機日期 50vh 可捲動，首月與次月標題可見；桌機／手機切換的過渡截圖已另取穩定畫面。測試分頁為自建 tab6，測後恢復淺色及預設 viewport，未操作使用者原分頁。

### 自測與剩餘工作

- 商戶 invariant tests 已增至 8 組且全過，新增同日匯率／非法值與過期對帳版本／鎖定動作測試。
- 最新 vue-tsc --noEmit 已通過（退出碼 0）；production build 仍留第 5 步集中驗證。
- 第一階段 UI 仍未全結案：要補全部明細／對話框、表格橫捲與頁尾實際操作、手機深色修後複驗，以及獨立 QA。
- 下一批依序：完整互動／表單／回放；通用金額精度與未結算派彩等剩餘資料項；會話清理；最新 build 與其他後台回歸。
- 未新增正式後端／持久化／角色規格，未 commit、push 或部署。

## 結論與版本邊界

- 12 個商戶入口均已接上實際元件，不再是 stage-page；主要前端 mock 原型已實作，但整體仍為「部分完成」，不是全部驗收通過。
- 本輪核對本地 HEAD 為 fec1f25，前一保存點為 dca1cc3。協調任務回報線上版本為 fec1f25；本輪未另行驗證遠端部署。
- 本地尚有未推送的關閉遊戲修正、報表共用計算與測試、日期搜尋及共用間距。不能視為線上已有。
- 本輪商戶六組測試、第一階段路由隔離測試、vue-tsc 均通過。最新工作樹尚未重新 production build；先前成功 build 不覆蓋最新增量。
- 獨立 QA 已由協調任務安排，但尚未取得逐頁通過／失敗證據，不能以開發自測代替。

## 最新規格邊界

- 遊戲只可查看與關閉本商戶平台遊戲；沒有任何遊戲申請／申請紀錄，不修改 RTP、限紅、全域或其他商戶。重新開啟規則待確認。
- 串接僅資訊與狀態查看，没有申請流程或紀錄；不暴露秘密、不假造測試成功。
- 角色細權、員工線路範圍、金鑰流程、正式結算公式／週期／確認層級、歷史保留、敏感操作送審不得自行定案。
- 其他模組申請僅本地原型，未接正式審批；邀請只保存草稿，未寄信／授權。前端資料投影不等於後端安全授權。

## 檔案索引

路徑相對於 C:/Users/user/Desktop/Asggg/game-admin：

| 簡稱 | 檔案 |
| --- | --- |
| R | src/router/modules/partnerPortals.ts |
| S | src/store/modules/merchantPortal.ts |
| C | src/views/partner-portals/merchant/commerce/index.vue |
| T | src/views/partner-portals/merchant/transactions/index.vue |
| P | src/views/partner-portals/merchant/reports/index.vue |
| O | src/views/partner-portals/merchant/operations/index.vue |
| Q | src/views/partner-portals/merchant/components/ScopedTable.vue |
| U | src/utils/merchantReporting.ts |
| I | scripts/merchant-portal-stage3.test.ts |

## 12 模組盤點

路由均以 /merchant/ 起始。狀態以整個模組交付判定；「已實作」僅指表內功能。所有模組的獨立驗收均尚未結案。

| 模組／路由 | 狀態／已實作 | 檔案及開發自測 | 尚未執行／待規格 |
| --- | --- | --- | --- |
| 儀錶板 dashboard | 部分完成；同源分幣摘要、待辦／報表入口 | R/S/P/U；聚合測試通過 | 摘要逐項核對、待辦跳轉及完整 UI |
| 遊戲 games | 部分完成；授權列表、明細、關閉本平台；無申請 | R/S/C；I 關閉不影響 RTP／限紅／其他商戶／歷史；先前 UI 搜尋 DRAGON 與返回保留搜尋 | 關閉確認／取消 UI；更新時間核對；試玩來源未提供；重開待確認 |
| 線路 lines | 部分完成；列表配置、新增／變更原型申請與去重 | R/S/C；I 外部線路拒絕、申請不提前生效 | 自由文字表單的結構化欄位核對；新增／錯誤／重複／返回 UI；正式審核未接 |
| 串接 integrations | 部分完成；資訊、指紋、既有測試狀態唯讀，無申請 | R/S/C；I 白名單與拒絕舊申請；先前 UI 兩個授權 Sandbox 項目 | 核准文件／測試端點／跳轉紀錄未提供；金鑰待確認 |
| 會員 members | 部分完成；會員、限制、餘額來源、關聯交易、申覆原型 | R/S/T/Q；I 範圍測試；先前僅到達檢查 | 明細、申覆、篩選分頁返回 UI；通用金額／狀態呈現核對 |
| 交易 transactions | 部分完成；注單／資金、明細、異常回報、單一重播與盤面頁籤、對外結果 | R/S/T/Q；I 保存結果版本不變；先前 B00003 保存 5×3 盤面，B00015 越權拒絕 | 播放／暫停／重啟／離開清理全測；資金／回報 UI；未結算派彩與金額精度核對 |
| 獎池 jackpots | 部分完成；自身參與、流水派發、變更原型申請 | R/S/O；I 空授權不回退；先前到達檢查 | 詳情與申請 UI；共享水位公開範圍待確認 |
| 營運報表 reports/operations | 部分完成；三維度、六日期快捷、排除測試、分幣總計、CSV、參考換算 | R/S/P/U；I 日期／去重／總計／CSV／缺匯率通過；先前八月 UI／下載一致 | 多幣、含測試、換算成功 UI；逐筆環境來源缺失；最新手機日期複驗 |
| 匯率 reports/exchange-rates | 部分完成；最新鎖定匯率、歷史快照唯讀 | R/S/O；I 私密欄位檢查 | 列表／快照連結／空資料 UI；同日多版本選取核對 |
| 對帳 reconciliation | 部分完成；自身對帳／差異／結算單、版本快照、文字回報；確認停用 | R/S/O；I 範圍檢查；先前到達檢查 | 差異 UI、版本競態測試、附件未接、對外處理摘要缺失；正式金額／確認／付款待規格 |
| 公告 notifications | 部分完成；示範公告／本地通知、詳細與已讀 | R/S/O；先前到達檢查 | 已讀未讀 UI；正式發布來源、持久化與歷史未接 |
| 帳號 access | 部分完成；示範人員、邀請草稿、權限說明、局部日誌 | R/S/O；先前 UI 看見示範人員；程式未寄信／未授權 | 草稿空值／重複／會話清理測試；細權與線路矩陣待定；正式邀請／停用／完整稽核未接 |

## 驗證證據與限制

### 本輪重新執行

- scripts/merchant-portal-stage3.test.ts：6 組全數通過。
  1. 商戶／線路交集與偽造範圍，不回退全資料。
  2. 關閉只改自身可用性，不改 RTP／限紅／其他商戶／歷史。
  3. 遊戲與串接舊申請拒絕，線路去重且不生效。
  4. 串接／Replay／匯率白名單與歷史版本不變。
  5. 日期、測試排除、分幣去重、局數會員數及跨維度總計。
  6. CSV 同源精度；缺匯率不產生部分合計。
- scripts/partner-portal-stage1.test.ts：navigation and isolation checks passed。
- vue-tsc --noEmit：退出碼 0。

### 先前開發 UI 自測，非本輪重跑或獨立 QA

- 桌機：遊戲搜尋／明細／返回、串接無申請、八月報表與本月空資料、保存盤面、外商戶注單拒絕。
- 八月排除測試資料為 TWD 6 局、1 位線路會員，投注 918／派彩 836／遊戲輸贏 82，依當時設定精度呈現。
- 實際 CSV：C:/Users/user/Downloads/merchant-report-2026-08-01_2026-08-31.csv；先前內容與 UI 一致，本輪確認仍存在、377 bytes。
- 部分頁面僅到達檢查，不可宣告互動全過。
- 390px 曾發現日期欄與雙月彈層擠壓，已改全寬與直排；最後 50vh 與共用間距仍待畫面複驗。
- 截圖僅在工具對話，未保存完整本地截圖附件；尚無完整 12 頁桌機／手機／明暗驗收矩陣。

## 本地未推送範圍

- S：關閉改為只變更可用性，避免一般更新流程改寫 RTP。
- U、I：同源報表計算／CSV 與六組測試。
- Q、T、P：整合日期搜尋、授權詳情、報表網址條件。
- src/views/partner-portals/merchant/date-picker.scss：手機全寬、雙月直排、50vh 可捲動彈層。
- src/assets/styles/core/merchant-layout.scss、src/assets/styles/index.scss、C：協調任務加入共用間距／對齊並移除廣泛 form 空白規則。保留其他協作者修改。
- 本進度文件兩份同步更新。未自行提交／推送。

## 未執行任務與依序開發流程

| 順序 | 工作 | 依賴／邊界 | 每步驗收條件 |
| --- | --- | --- | --- |
| 1 | 12 頁桌機／390px／明暗 UI，欄名、卡片間距、彈窗、日期、表格橫捲 | 等獨立 QA 具體證據；協調共用樣式 | 每頁各尺寸主題有結果；不遮操作、頁面不溢出、表格可捲，修後複驗 |
| 2 | 補齊查詢／重置／分頁／返回、空資料／錯誤／偽造 ID、表單及重播全操作 | mock 可測，不替未定規則做決定 | 每模組步驟與結果；失敗不改來源、不越權、不回退全部 |
| 3 | 核對金額精度、未結算派彩、狀態文字、待辦連結、同日匯率版本、差異版本競態 | 先按已定規格判斷；新規則請使用者決定 | 各項有結論、修復及測試；不把未結算零值當正式派彩、不默默使用錯版 |
| 4 | 登入／登出／重整／角色切換及草稿／已讀／日誌生命週期 | 示範固定身分；持久化政策需確認 | 區分預期重整遺失與缺陷；不得跨會話殘留他人可見資料 |
| 5 | 最新完整 build、管理者／代理回歸、下載／重播回歸 | 合併已確認修復後 | build／測試通過，人工證據完整，QA 缺陷複驗或明確接受 |
| 6 | 使用者確認未定規格，形成決策表 | 細權／線路／金鑰／結算／歷史／敏感審批 | 每項有決策、例外、驗收條件；未定操作仍唯讀或停用 |
| 7 | 另行授權後正式後端、持久化、跨後台審批聯動 | 後續階段；需契約、後端授權及測試環境 | 伺服器驗證範圍；申請不提前生效；完整稽核、不暴露秘密、不假付款 |
| 8 | 交付與部署版本核對 | 由協調任務管理提交／部署 | 版本、證據、限制與線上相符；本任務不自行 push |

## 尚未完成的正式能力

- 示範固定商戶／線路投影不等於正式會話注入、後端授權與员工權限。
- 申請、已讀、草稿、日誌為記憶體資料；重整不保證保存。未接正式郵件、附件或平台審批。
- 對帳能看來源與快照，不代表正式應收應付、版本確認、付款或鎖定已完成。
- 文件、Sandbox、金鑰未提供，不補造；共享獎池水位、歷史保留仍待決策。
- 報表是 mock，缺逐筆環境標記；遊戲輸贏不是營收，參考換算不替代結算快照。

## 完成定義

先完成已確認原型與集中自測、獨立 QA，再由使用者確認未定規格；正式聯動另行安排。每次交付必列版本、檔案、證據與缺口，不以頁面存在、服務開啟或型別檢查通過宣告全部完成。
